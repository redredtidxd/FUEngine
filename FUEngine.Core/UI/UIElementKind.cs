namespace FUEngine.Core;

/// <summary>Tipo de elemento UI.</summary>
public enum UIElementKind
{
    Canvas,
    Button,
    Text,
    Image,
    Panel,
    /// <summary>Contenedor de pestañas (Lua + propiedades en inspector).</summary>
    TabControl
}
