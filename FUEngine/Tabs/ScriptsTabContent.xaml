<UserControl x:Class="FUEngine.ScriptsTabContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FUEngine"
             Background="#161b22">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" MinWidth="180"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#21262d" Padding="8" BorderBrush="#30363d" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Scripts del proyecto" FontWeight="SemiBold" Foreground="#e6edf3" Margin="0,0,0,8"/>
                <ListBox x:Name="ScriptsList" Grid.Row="1" Background="Transparent" Foreground="#e6edf3" 
                         SelectionChanged="ScriptsList_OnSelectionChanged"
                         MouseDoubleClick="ScriptsList_OnMouseDoubleClick">
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Abrir" Click="ScriptsContext_Open"/>
                            <MenuItem Header="Abrir en externo" Click="ScriptsContext_OpenInExternalEditor"
                                      ToolTip="Usa el .exe configurado en EngineSettings.ExternalCodeEditorPath. Si no existe, abre en el explorador."/>
                            <MenuItem Header="Abrir ubicación en explorador" Click="ScriptsContext_OpenInExplorer"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </Grid>
        </Border>
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#30363d"/>
        <TabControl x:Name="ScriptsTabs" Grid.Column="2" Background="#0d1117" BorderThickness="0"
                    Padding="0" Margin="0"/>
    </Grid>
</UserControl>
