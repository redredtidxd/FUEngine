<Window x:Class="FUEngine.Dialogs.BuildExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exportar build del juego"
        Width="540"
        Height="460"
        WindowStartupLocation="CenterOwner"
        Background="#0d1117"
        Foreground="#e6edf3"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Genera una carpeta con el ejecutable del motor, la carpeta Data (proyecto + assets) y proyecto.json con la escena inicial."
                   TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#8b949e" FontSize="11"/>

        <TextBlock Grid.Row="1" Text="Escena inicial" Margin="0,0,0,4"/>
        <ComboBox x:Name="CmbScene" Grid.Row="2" Margin="0,0,0,10" Background="#161b22" Foreground="#e6edf3"/>

        <TextBlock Grid.Row="3" Text="Nombre del ejecutable (sin .exe)" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtExeName" Grid.Row="4" Margin="0,0,0,10" Background="#161b22" Foreground="#e6edf3" CaretBrush="#e6edf3"/>

        <TextBlock Grid.Row="5" Text="Carpeta de salida" Margin="0,0,0,4"/>
        <Grid Grid.Row="6" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtOutput" Grid.Column="0" Margin="0,0,8,0" Background="#161b22" Foreground="#e6edf3" CaretBrush="#e6edf3" IsReadOnly="True"/>
            <Button Grid.Column="1" Content="…" Width="36" Click="BtnBrowse_OnClick" Padding="4,2"/>
        </Grid>

        <CheckBox x:Name="ChkPublish" Grid.Row="7" Margin="0,0,0,8" Foreground="#c9d1d9">
            <TextBlock Text="dotnet publish (win-x64, self-contained) en la carpeta de salida — requiere SDK" TextWrapping="Wrap"/>
        </CheckBox>

        <TextBox x:Name="TxtLog" Grid.Row="8" Background="#010409" Foreground="#7ee787"
                 FontFamily="Consolas" FontSize="10" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" IsReadOnly="True" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnExport" Content="Exportar" Width="100" Margin="0,0,8,0" Click="BtnExport_OnClick" IsDefault="True"/>
            <Button Content="Cancelar" Width="80" Click="BtnCancel_OnClick" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
