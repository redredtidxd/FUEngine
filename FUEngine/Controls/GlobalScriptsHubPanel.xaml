<UserControl x:Class="FUEngine.GlobalScriptsHubPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#161b22"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="180"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#21262d" Padding="8" BorderBrush="#30363d" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Scripts globales" FontWeight="SemiBold" Foreground="#e6edf3" Margin="0,0,0,4"/>
                <TextBlock x:Name="TxtScriptsRoot" Grid.Row="1" FontSize="10" Foreground="#8b949e" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <ListBox x:Name="ScriptsList" Grid.Row="2" Background="Transparent" Foreground="#e6edf3"
                         SelectionChanged="ScriptsList_OnSelectionChanged"
                         MouseDoubleClick="ScriptsList_OnMouseDoubleClick"/>
            </Grid>
        </Border>
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#30363d"/>
        <TabControl x:Name="ScriptsTabs" Grid.Column="2" Background="#0d1117" BorderThickness="0" Padding="0" Margin="0"/>
    </Grid>
</UserControl>
